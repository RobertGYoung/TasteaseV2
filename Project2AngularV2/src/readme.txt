entry point is login component through an empty redirect in app-routing.module
    -before it gets there, the main app component includes an index component which contains the 
        routing outlet and a footer.
    the login component has the first header component- this header goes the second header upon logining in and accessing the dashboard
    -registration 